# TODO — 改善バックログ

## すぐやる (着手順)

- [描画] 拒否された Edit の diff を表示する (parse 層では計算済みだが permission_deny の描画が捨てている。「何を拒否されたか」はむしろ一番見たい情報)
- [描画] Write の内容を diff ぽく描画する (↑で立てた差分描画の部品に乗せる)
- [描画] Edit / Write ツールの成功時の結果は描画しない (successfully みたいなやつ)

ここまでの3件は Edit / Write の同じ描画ブロックを触るため、まとめて片付ける。

- [描画] Read の Input もいらないかも？ (消す前に実物を確認して判断)
- [描画] ユーザー呼び出しによるスキル発動時も、スキルチップUIは発話の外に描画したい。。。(ごめん)
- [調査→描画] Bash ツールの他のフィールドもUIに組み込みたい
  - Bash ツールのサンドボックス有無を拾う (現在の挙動は要確認)
- [新機能] サブエージェント単体をレンダリングできるようにする [^2]

## いずれやる

- [新機能] HTML 出力でマークダウンをレンダリングしたいかも。。。
  - 出力構造そのものが変わるため、CSS の書き直しより先に決着させる
- [デザイン] CSS が下手すぎるので /html-css-js-coding で書き直し
  - 描画まわりの変更が落ち着いてから着手
- [新機能] サブエージェントのツール実行を本流にインライン埋め込みする [^2]
  - [内部品質] ToolCall まわりのモデルのリファクタリング [^1]
  - `Event` が再帰構造になるため、設計書どおりリファクタとセットで着手する

## 要検討

- 翻訳セグメントの誤分割リスク (PR #9 レビューの残り)
  - 翻訳対象の本文中にマーカー文字列 `<<<CCRENDER-SEG n>>>` が行として含まれていると誤分割される。発生頻度は低く現状の実装・テストで許容 (Codex 判断) だが、根本対応するならエスケープや衝突しない区切りの生成など

---

## 各詳細

### ToolCall まわりのモデルのリファクタリング

**問題**: `internal/parse/model.go` の `ToolCall` に濁りが溜まりつつある。

- permission_deny の事実が `Event.Kind` と `ToolCall.DenyReason` の2階層に割れており、通常のツール呼び出しでは `DenyReason` が死にフィールドになる
- `HasResult` + `Result` + `IsError` は手作りの Option 型で、無効な組み合わせ (`HasResult: false, IsError: true`) が型の上で表現できてしまう
- `Summary` (ツール別の表示用要約) や `Input` (整形済み JSON 文字列) など描画の前処理が parse 層に染み出しており、`Diff` の追加で「特定ツールのときだけ意味を持つ表示用文字列」がさらに増える

**方針**: 現在の規模では直しに行かない。
「同一ファイルへの連続 Edit の折り畳み」や「サブエージェントのツール実行の描画」など Event 列の構造に手が入るタイミングで、ToolCall のライフサイクル (呼び出し/結果あり/結果なし/拒否) の型表現の見直しと、表示用フィールドの render 層への移動をあわせて検討する。

なお `Event` の Kind + ポインタ3本というタグ付き union 形と `EventKind` の型エイリアスは、`text/template` に直接渡す制約 (`eq` 比較) の帰結であり、対象外とする。

### サブエージェントのツール実行と権限拒否を拾う

**動機**: 現在サブエージェントから拾っているのは `AgentID` / `AgentType` / `Prompt` / 最終回答のみで、`agent-*.jsonl` の中身は最後の assistant テキストしか読んでいない (`internal/parse/subagent.go`)。サブエージェントが何のツールを叩き、何を拒否されたかが一切見えない。

**現状の把握**:

- サブエージェント JSONL は本流とまったく同じ構造で、`readRecords` には `keepSidechain` 引数が既にある。`buildEvents` はそのまま流用できる
- ただし現在サブエージェント JSONL を直接渡しても読めない。本流読み込みが `isSidechain=true` を全て捨てるため「有効な行がありません (スキップ 0行)」になる (実測済み)

**方針**: 二段構えで進める。前段は後段の部分集合であり、手戻りが出ない。

1. **単体レンダリング (すぐやる)**
   `readRecords` の sidechain 判定を入力に応じて切り替え、`agent-*.jsonl` を通常のセッションとしてレンダリングできるようにする。
   あわせて `locate` にセッションID + エージェントIDでの指定を足す。描画側は無変更。ToolCall リファクタを巻き込まないため単独で着手できる
2. **インライン埋め込み (いずれやる)**
   `Subagent` に `Events []Event` を持たせ、テンプレートの描画ブロックを `{{define}}` に切り出して再帰呼び出しする。HTML では `<details>` で折りたたみ、罫線でネストを表現。
   `Event` が再帰構造になるため ToolCall リファクタとセットで着手する。出力量が膨らむ点にも注意

[^1]: #toolcall-まわりのモデルのリファクタリング
[^2]: #サブエージェントのツール実行と権限拒否を拾う
