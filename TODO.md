# TODO — 改善バックログ

実際の開発セッションを ccrender で描画した際に、利用エージェントから寄せられたフィードバックをもとにした改善候補の一覧。優先度の高いものから並べている。

## 1. テキストなしのアシスタントターンで見出しが抜ける

**問題**: 前置きテキストなしでツールだけを呼んだアシスタントターンでは「🤖 Assistant」見出しが描画されず、ツール呼び出しが直前のユーザー発言にぶら下がって見える。読み手には「ユーザーがツールを実行した」ように見え、発言の帰属が壊れる。

**改善案**: テキストが空でも見出しは常に出力する。

## 2. Edit の変更内容が見えない

**問題**: Edit ツールの描画が結果メッセージ (「updated successfully」) のみで、何をどう変えたのかが失われる。編集が連続するセッションでは、最も情報量の多い部分が空白になる。

**改善案**: 結果ではなく入力 (`old_string` → `new_string`) を要約表示する。長い場合は変更前後それぞれ先頭数行 + 省略で足りる。同一ファイルへの連続 Edit を「Edit ×N — 同一ファイル」と折り畳む案もあわせて検討する。

## 3. 複数行の Bash コマンドが1行目で切れる

**問題**: heredoc を使ったコマンド (例: `git commit -m "$(cat <<'EOF'`) が1行目で切れ、コマンドの本文が読めない。

**改善案**: 複数行コマンドはコードフェンスで全文表示する。

## 4. ヘッダー統計にモデル名がない

**問題**: どのモデルで実行されたセッションかをヘッダーから確認できない。観測ログとの突合ではモデル名が必須項目になる。

**改善案**: ヘッダー統計にモデル名を追加する。

## 5. 絶対パスの繰り返しが冗長

**問題**: ヘッダーでプロジェクトルートを宣言しているにもかかわらず、各ツール呼び出しの表示が絶対パスのままで行が長い。AI が読む用途ではトークンの無駄にもなる。

**改善案**: プロジェクトルート配下のパスは相対パスに短縮して表示する。
